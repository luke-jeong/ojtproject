spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mytest
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: root

  jpa:
    hibernate:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
#
#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type: trace